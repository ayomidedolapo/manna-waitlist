datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Waitlist {
  id        String   @id @default(cuid())
  name      String   @unique
  email     String   @unique
  phone     String?
  product   String
  frequency String
  createdAt DateTime @default(now())
}